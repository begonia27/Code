#include <stdio.h>

int main(void)
{
    const char string[9] = "two words";
    int i;

    for (i = 0; i >= 4 && i < 20; ++i)
    {
        printf("result is %c", string[9]);
    }

    return 0;
}
