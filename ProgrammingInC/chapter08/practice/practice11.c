// 接收一个数组及其长度为参数，返回值是参数数组中所有元素的和

#include <stdio.h>

/*int arraySum(int arraySum[3])
{   
    int i, result;
    int arraySum[0] = {2};
    int arraySum[1] = {3};
    int arraySum[2] = {4};

    for (i = 0; i < 3; ++i)
    {
        result += arraySum[i];
    }

    return result;
}*/

int main(void)
{   
    int arraySum[i];

    int i, result;
    int arraySum[0] = {2};
    int arraySum[1] = {3};
    int arraySum[2] = {4};

    for (i = 0; i < 3; ++i)
    {
        result += arraySum[i];
    }

    printf("%i\n");

    return 0;
}
